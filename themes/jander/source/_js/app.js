var loader = require('./loader');
var $ = require('jquery');
var _ = require('lodash');

$(function () {
    // Initialize Posts
    $('.posts').each(function () {
        // TODO
    });

    // Load Page Modules
    loader();
});